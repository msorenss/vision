# Frontend (Next.js UI)

Minimal UI for uploading an image and rendering bounding boxes.

## Quick start

```powershell
npm install
$env:NEXT_PUBLIC_API_BASE = "http://localhost:8000"
npm run dev
```

Then open http://localhost:3000
